// An expression is a statement that returns a value. So if it can appear on the right side of an assignment,
//  or as a parameter to a method call, it is an expression.

const x = 1 + 1;

const isTrue = true ? "yes" : no;

const fn = function () {
  return "Hello world";
};

const result = anotherFunction();

const anotherFunction = () => {
  return "Hello world 2";
};

//

// const y = if(true){

// }

// const loop = for(let i = 0; i < 10 ; i++){

// }
